rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fully open storage rules for testing as requested
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}